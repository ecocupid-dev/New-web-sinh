** Projects:
  - filter:
    + Title (input)
    + Project (select)
    + Category (select)
    + IsFeature (select)
    + CreatedBy (select - users)
    + Created (date picker)

** Users:
  - filter:
    + UserName (input)
    + Email (input)
    + Position (select)
    + Created (date picker)

** Category:
  - filter:
    + Name (input)
    + CreatedBy (select - users)

** Video:
  - filter:
    + Title (input)
    + Project (select)
    + CreatedBy (select - users)
    + Created (date picker)
    + IsFeature (select)

** Project:
  - filter:
    + Title (input)
    + Country (select - local)
    + Created (date picker)